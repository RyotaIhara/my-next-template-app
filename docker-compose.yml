version: "3.9"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    # ローカルのコード変更をコンテナに反映させる（ホットリロード用）
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      NODE_ENV: development

volumes:
  node_modules:
